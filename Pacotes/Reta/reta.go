package main

// import (
// 	"math"
// )

// type Ponto struct {
// 	x float64
// 	y float64
// }

// //funções, variáveis ou estruturas que tem o seu nome iniciado com letra minuscula, tem sua visibilidade PRIVADA
// //funções, variáveis ou estruturas que tem o seu nome iniciado com letra maiuscula, tem sua visibilidade PÚBLICA

// func catetos(a, b Ponto) (cx, cy float64) {
// 	cx = math.Abs(b.x - a.x) //ponto representa uma coordenada no plano cartesiano
// 	cy = math.Abs(b.y - a.y)
// 	return

// }

// func Distancia(a, b Ponto) float64 {
// 	cx, cy := catetos(a, b)
// 	return math.Sqrt(math.Pow(cx, 2) + math.Pow(cy, 2))
// }
